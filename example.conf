# A list of classes to apply
"classes": [
  "singleton::test",
]

# Data to use when building the catalog
"data": {
  "singleton::test::arg1": "example",
  "singleton::test::arg2": "like hiera data",
}

# A list of required Forge modules
"modules": {
  "puppetlabs/stdlib": {
    "version": "4.8.0",
  },
}
