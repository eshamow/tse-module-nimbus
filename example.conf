# A list of classes to apply
"classes": [
  "workstation::test",
]

# Data to use when building the catalog
"data": {
  "workstation::test::arg1": "example",
  "workstation::test::arg2": "like hiera data",
}

# A list of required Forge modules
"modules": {
  "puppetlabs/stdlib": {
    "version": "4.8.0",
  },
}
